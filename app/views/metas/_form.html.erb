
<%= form_for @meta , :class=>"form-horizontal" , :remote => true do |f| %>
  <% if @meta.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@meta.errors.count, "error") %> prohibited this meta from being saved:</h2>

      <ul>
      <% @meta.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
 <%= f.hidden_field :proyecto_id, :value=> @proyecto.id %>
  <div class="field">
    <%= f.label :detalle %>
    <%= f.text_field :detalle %>
  </div>
  <div class="field">
    <%= f.label :estado %>
    <%= f.text_field :estado %>
  </div>
  <div class="actions">
     <%= f.submit 'Guardar', :class=>'btn btn-primary'%>
	<%= link_to 'Cancelar', {:controller=>"proyectos",:action => 'cancelar_form_meta'}, :remote=> true , :class=>'btn btn-primary' %>
  </div>
<% end %>
